import { ReactComponent as Spinner } from '../icons/spinner.svg'
import styles from './GlobalSpinner.module.scss'

const GlobalSpinner = () => <Spinner className={styles.GlobalSpinner} />

export default GlobalSpinner
